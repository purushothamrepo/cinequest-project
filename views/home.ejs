<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CineQuest</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
    <style>
        :root {
            --primary: #ff2d55;
            --dark: #1a1a1a;
            --darker: #0f0f0f;
            --light: #ffffff;
            --grey: #8a8a8a;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--darker);
            color: var(--light);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .page-wrapper {
            flex: 1 0 auto;
            display: flex;
            flex-direction: column;
            width: 100%;
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        .hero {
            flex: 1;
            background: linear-gradient(135deg, #1a1a1a 0%, #0f0f0f 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 4rem 0;
            position: relative;
            overflow: hidden;
            min-height: 70vh;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,45,85,0.1) 0%, transparent 70%);
            animation: pulse 10s infinite;
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.2); }
            100% { transform: scale(1); }
        }

        .hero-content {
            text-align: center;
            position: relative;
            z-index: 1;
            max-width: 800px;
            width: 100%;
        }

        .hero h1 {
            font-size: clamp(2.5rem, 5vw, 3.5rem);
            font-weight: 700;
            margin-bottom: 1.5rem;
            background: linear-gradient(45deg, var(--primary), #ff6b6b);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .hero p {
            font-size: clamp(1rem, 2vw, 1.2rem);
            color: var(--grey);
            margin-bottom: 2rem;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .search-container {
            max-width: 600px;
            width: 100%;
            display: flex;
            gap: 1rem;
            background: rgba(255, 255, 255, 0.05);
            padding: 0.5rem;
            border-radius: 50px;
            backdrop-filter: blur(10px);
            margin: 0 auto;
        }

        .search-input {
            flex: 1;
            background: transparent;
            border: none;
            color: var(--light);
            padding: 1rem 1.5rem;
            font-size: 1.1rem;
            outline: none;
        }

        .search-input::placeholder {
            color: var(--grey);
        }

        .search-btn {
            background: var(--primary);
            border: none;
            padding: 1rem 2rem;
            border-radius: 50px;
            color: var(--light);
            font-weight: 600;
            transition: transform 0.3s ease;
        }

        .search-btn:hover {
            transform: scale(1.05);
            background: #ff6b6b;
        }

        .features {
            padding: 4rem 0;
            background: var(--dark);
            text-align: center;
            width: 100%;
        }

        .features h2 {
            font-size: clamp(2rem, 4vw, 2.5rem);
            font-weight: 700;
            margin-bottom: 3rem;
        }

        .feature-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        .feature-card {
            padding: 2rem;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 20px;
            transition: transform 0.3s ease;
        }

        .feature-card:hover {
            transform: translateY(-10px);
        }

        .feature-card h3 {
            font-size: 1.3rem;
            margin-bottom: 0.5rem;
        }

        .feature-card p {
            color: var(--grey);
            font-size: 1rem;
        }

        footer {
            width: 100%;
            padding: 2rem;
            text-align: center;
            color: var(--grey);
            background: var(--darker);
            border-top: 1px solid rgba(255,255,255,0.1);
            flex-shrink: 0;
        }
    </style>
</head>
<body>
    <div class="page-wrapper">
        <div class="hero">
            <div class="hero-content">
                <h1>Cinequest</h1>
                <p>Explore movies and TV shows with curated insights and recommendations</p>
                <form action="/result" method="GET" class="search-container">
                    <input type="text" class="search-input" name="movieName" placeholder="Search movies or shows..." required>
                    <button type="submit" class="search-btn">Search</button>
                </form>
            </div>
        </div>

        <section class="features">
            <h2>Why Cinequest?</h2>
            <div class="feature-grid">
                <div class="feature-card">
                    <h3>Curated Insights</h3>
                    <p>Discover hidden details about your favorite titles</p>
                </div>
                <div class="feature-card">
                    <h3>Smart Search</h3>
                    <p>Find movies and shows with ease</p>
                </div>
                <div class="feature-card">
                    <h3>Rich Content</h3>
                    <p>Explore trivia, ratings, and more</p>
                </div>
            </div>
        </section>
    </div>

    <footer>
        &copy; 2025 CineQuest. Crafted for movie enthusiasts.
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.min.js"></script>
</body>
</html>